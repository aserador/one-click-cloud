export interface IGraphEdge {
  id: string;
  source: string;
  target: string;
}

export interface ISchema {
  id: string;
  continuous_deployment: {};
  cost: {};
  enabled: boolean;
  schema: {};
}